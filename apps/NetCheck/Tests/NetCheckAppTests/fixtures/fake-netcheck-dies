#!/usr/bin/env perl
# Stand-in for a `netcheck watch` that dies right after the handshake —
# exercises StatusFetcher's restart-with-backoff path.
use strict;
use warnings;
$| = 1;
print "{\"Hello\":{\"protocol_version\":1,\"config_watcher\":\"active\",\"healthy_interval_secs\":60,\"degraded_interval_secs\":10}}\n";
exit 0;
