#!/usr/bin/env bash
#MISE description="Build the full netcheck binary (dashboard + JSON subcommands, TUI enabled; pass --release for a release build)"
set -euo pipefail

cargo build -p netcheck --bin netcheck --features tui "$@"
