#!/usr/bin/env bash
#MISE description="Check Swift formatting with SwiftFormat"
#MISE sources=["apps/NetCheck/Sources/**", "apps/NetCheck/Tests/**", ".swiftformat"]
#MISE outputs={auto=true}
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

swiftformat --lint "$ROOT_DIR/apps/NetCheck/Sources" "$ROOT_DIR/apps/NetCheck/Tests"
