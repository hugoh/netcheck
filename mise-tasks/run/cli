#!/usr/bin/env bash
#MISE description="Run netcheck-cli (defaults to 'status'; pass another subcommand: mise run run:cli -- wifi)"
set -euo pipefail

if [ "$#" -eq 0 ]; then
	set -- status
fi

cargo run -p netcheck-cli -- "$@"
