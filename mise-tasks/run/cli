#!/usr/bin/env bash
#MISE description="Build and run the netcheck CLI (pass args after --)"
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

exec swift run --package-path "$ROOT_DIR/apps/NetCheck" netcheck "$@"
