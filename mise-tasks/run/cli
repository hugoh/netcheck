#!/usr/bin/env bash
#MISE description="Run a netcheck JSON subcommand (defaults to 'status'; pass another subcommand: mise run run:cli -- wifi)"
set -euo pipefail

if [ "$#" -eq 0 ]; then
	set -- status
fi

cargo run -p netcheck --bin netcheck --features tui -- "$@"
