#!/usr/bin/env bash
#MISE description="Build (if needed) and launch the dev NetCheck.app"
#MISE depends=["build:app"]
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

open "$ROOT_DIR/target/dev-app/NetCheck.app"
