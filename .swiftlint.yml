excluded:
  - apps/NetCheck/.build

# Short, single-purpose closure/map parameters (v, r, c, p, ok, on, fm, up,
# g) are this codebase's normal style throughout ContentView/PartialStatus/
# StatusFetcher — not a naming problem worth flagging.
identifier_name:
  min_length:
    warning: 1
    error: 1
